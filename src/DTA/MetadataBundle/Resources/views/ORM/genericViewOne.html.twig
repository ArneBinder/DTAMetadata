{# Renders a single database entity.
    
    Assumes that the object passed via the data variable is a propel object.
    Especially expects a getId method for highlighting.
 #}


{% block core %}
    {{ form_row(form, {'inlineLabel':true, 'hideLabel':true}) }}
    {{ form_rest(form) }}
{% endblock core %}
