imports:
    - { resource: parameters.yml }
    - { resource: security.yml }

framework:
    #esi:             ~
    #translator:      { fallback: %locale% }
    secret:          %secret%
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: %kernel.debug%
    form:            true
    csrf_protection: true
    validation:      { enabled: true, enable_annotations: true }
    templating:      { engines: ['twig'] } #assets_version: SomeVersionScheme
    default_locale:  %locale%
    trust_proxy_headers: false # Whether or not the Request object should trust proxy headers (X_FORWARDED_FOR/HTTP_CLIENT_IP)
    session:         ~

# Twig Configuration
twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%
    form:
        resources:
            - 'DTAMetadataBundle:Form:sortableCollection.html.twig'

# Assetic Configuration
assetic:
    debug:          %kernel.debug%
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: %kernel.root_dir%/Resources/java/compiler.jar
        #yui_css:
        #    jar: %kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar

# Doctrine Configuration
#doctrine:
#    dbal:
#        driver:   %database_driver%
#        host:     %database_host%
#        port:     %database_port%
#        dbname:   %database_name%
#        user:     %database_user%
#        password: %database_password%
#        charset:  UTF8
#
#    orm:
#        auto_generate_proxy_classes: %kernel.debug%
#        auto_mapping: true

propel:
    build_properties:
      propel.packageObjectModel: true
      propel.namespace.autoPackage: true
      propel.behavior.reconstructed_flaggable.class: src.DTA.MetadataBundle.Model.reconstructed_flaggable.ReconstructedFlaggableBehavior
      # propel.behavior.reconstructed_flaggable.class: vendor.propel-reconstructed_flaggable-behavior.ReconstructedFlaggableBehavior
#      propel.database: "%database_driver%"
#      propel.database.url: "%database_driver%:host=%database_host%;dbname=%database_name%;charset=%database_charset%"
#      propel.database.user: "%database_user%"
#      propel.database.password: "%database_password%"
#      if the correct namespace is provided, these settings are adjusted automatically
#      propel.output.dir: ../../../../src/DTA/MetadataBundle
#      propel.php.dir: ${propel.output.dir}/Model
#      propel.phpconf.dir: ${propel.output.dir}/Resources/conf
#      propel.sql.dir: ${propel.output.dir}/db/sql
    dbal:
        default_connection: DTAMetadata
        connections:
            DTAMetadata:
              driver:     "%database_driver%"
              user:       "%database_user%"
              password:   "%database_password%"
              dsn:        "%database_driver%:host=%database_host%;dbname=%database_name%;charset=%database_charset%"
              options:    {}
              attributes: {}
            ReverseSource:
              driver:     "%database_driver%"
              user:       "%database_user%"
              password:   "%database_password%"
              dsn:        "%database_driver%:host=%database_host%;dbname=metadatenbank;charset=%database_charset%"
           

        
# Swiftmailer Configuration
swiftmailer:
    transport: %mailer_transport%
    host:      %mailer_host%
    username:  %mailer_user%
    password:  %mailer_password%
    spool:     { type: memory }
